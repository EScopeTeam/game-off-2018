import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    position: "relative",
  },
  image: {
    position: "absolute",
    top: 0,
    left: 0,
    width: "100%",
    height: "100%",
  },
});
